program forprog;

var ch : char;
var ch : integer;

procedure test();
var a : integer;
begin
    println()
end;

procedure forme(a : integer, b : boolean, c : char);
var i, j : integer;
begin
    j := 0;
    j := j * 5 + 1;

    z := 3;

    for i := 0 to a do
    begin
        j := j * 5 + 1;
        printint(j);
        println()
    end
end;

begin
    sbrubbles();
    forme := 30;
    test();
    ch := '1';
    forme();
    forme(111, 222);
    forme(3, true, ch);
    forme(7, 8, z);
    test(2)
end.
